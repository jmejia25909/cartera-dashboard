# GESTION-SERVICE-FOUNDATION-PACK-016

Crea la capa inicial de servicios del módulo Gestión.

Incluye:

- búsqueda normalizada;
- ordenamiento genérico;
- paginación;
- conteos de resultados;
- cálculo reutilizable de totales.

Este paquete no modifica todavía `App.tsx`. Prepara la migración controlada de la lógica existente en el siguiente paso.
