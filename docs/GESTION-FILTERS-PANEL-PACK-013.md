# GESTION-FILTERS-PANEL-PACK-013

Encapsula el bloque completo de filtros y acciones del módulo Gestión.

Este paso:

- crea `GestionFiltersPanel`;
- conserva controles, eventos y estados actuales;
- evita cambios funcionales;
- prepara la extracción posterior de props y handlers;
- mantiene el diseño existente mediante `display: contents`.
